# DOM classList Property

##  What is classList?

`classList` is a DOM property used to work with an element's `class` attribute.

It returns a `DOMTokenList` object that provides methods for adding, removing, toggling, checking, and replacing CSS classes.


## classList Methods

- classList.add()
- classList.remove()
- classList.toggle()
- classList.contains()
- classList.replace()